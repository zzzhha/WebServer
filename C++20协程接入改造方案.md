# C++20 协程接入改造方案（WebServer）

## 1. 目标与边界
- 目标：在保持现有功能（TLS、路由、下载/静态文件、MySQL、Phase2/3 worker链路）不退化的前提下，引入 C++20 协程，降低回调复杂度、提升吞吐与尾延迟表现。
- 约束：允许大幅改造，因此本文提供 3 条可行路径（满足“至少两种”），覆盖渐进到全量重构。
- 当前关键锚点（基线）：
  - 连接建立与上下文挂载：[HttpServer.cpp](file:///home/zsy/WebServer/cppBackend/reactor/HttpServer.cpp#L72-L85)
  - IO收包→worker处理→IO回写主链路：[HttpServer.cpp](file:///home/zsy/WebServer/cppBackend/reactor/HttpServer.cpp#L92-L470)
  - EventLoop 驱动与 queueinloop：[Eventloop.cpp](file:///home/zsy/WebServer/cppBackend/reactor/Eventloop.cpp#L31-L108)
  - 连接读写/TLS/sendfile：[Connection.cpp](file:///home/zsy/WebServer/cppBackend/reactor/Connection.cpp#L86-L426)
  - MySQL 连接池阻塞点（`sem_wait`）：[sqlconnpool.cpp](file:///home/zsy/WebServer/cppBackend/mysql/sqlconnpool.cpp#L39-L53)
  - 静态/下载阻塞 `stat/open` 路径：[StaticFileService.cpp](file:///home/zsy/WebServer/cppBackend/services/src/StaticFileService.cpp#L42-L147)、[DownloadService.cpp](file:///home/zsy/WebServer/cppBackend/services/src/DownloadService.cpp#L96-L211)
  - 现有 CMake 为 C++17：[CMakeLists.txt](file:///home/zsy/WebServer/CMakeLists.txt#L1-L25)、[cppBackend/CMakeLists.txt](file:///home/zsy/WebServer/cppBackend/CMakeLists.txt#L1-L239)

---

## 2. 协程库选型对比与集成步骤

### 2.1 选型对比（cppcoro / folly::coro / libunifex）
| 维度 | cppcoro | folly::coro | libunifex |
|---|---|---|---|
| 成熟度 | 高（经典 task/async generator） | 很高（Meta生产级） | 中高（先进但学习曲线高） |
| 调度模型 | task + thread pool，易封装 | executor + coroutine 丰富 | Sender/Receiver 最强组合能力 |
| 与现有 Reactor 兼容 | 高（最适合渐进接入） | 中（依赖栈较重） | 中（适合重构式改造） |
| 依赖复杂度 | 低-中 | 高（folly/gflags/glog/fmt 等） | 中（CMake FetchContent可接） |
| 推荐场景 | 方案A（渐进） | 方案B（折中） | 方案C（全量） |

### 2.2 推荐结论
- 推荐优先级：A（cppcoro） > C（libunifex） > B（folly::coro）
- 原因：
  - 当前代码是 Reactor + ThreadPool + Phase2/3 异步链路，A 的侵入最小，风险最可控。
  - C 适合“允许大幅改造”前提，可获得最好的长期架构一致性。
  - B 工程依赖重，适合作为组织内已有 folly 生态时的选择。

### 2.3 集成步骤（按库）
- `cppcoro`：
  - CMake 增加 `FetchContent(cppcoro)` 或子模块。
  - 新增 `CoroutineRuntime`（Task/Scheduler/CancelToken）并先只接入 `HttpServer` 主链路。
- `folly::coro`：
  - 先落地 folly 依赖（fmt/boost/context/openssl 等），再引入 `folly::coro::Task`。
  - 从 `HandleMessageInWorker` 开始改为 `co_await` 工作流。
- `libunifex`：
  - `FetchContent(libunifex)` + 独立 executor 层。
  - 以 Sender/Receiver 重写 IO→CPU→IO 三段流水线。

---

## 3. 方案 A（渐进式，推荐）：cppcoro + 保留现有 Reactor

### 3.1 改造思路
- 保留 `EventLoop/Epoll/Channel/Connection` 主体，不推翻现有结构。
- 将“回调+共享状态机”逐步替换为“协程函数+awaitable”：
  - 第一步：`HttpServer::HandleMessageInWorker` 协程化。
  - 第二步：`SqlConnPool` 与文件IO变为协程 await。
  - 第三步：路由 handler 支持 `Task<HttpResponse>`。

### 3.2 方案A逐文件改造清单（位置 + 内容）
| 文件 | 具体位置 | 改动内容 |
|---|---|---|
| [Eventloop.h](file:///home/zsy/WebServer/cppBackend/reactor/Eventloop.h#L21-L68) | `class EventLoop` | 新增 `RegisterFdAwaiter/ResumeFdAwaiters/ScheduleCoroutine` 接口，维护 `fd -> coroutine_handle` 映射。 |
| [Eventloop.cpp](file:///home/zsy/WebServer/cppBackend/reactor/Eventloop.cpp#L31-L56) | `run()` 主循环 | 在 `ch->handleevent()` 前后加入协程唤醒分发；`queueinloop` 兼容 coroutine resume。 |
| [Connection.h](file:///home/zsy/WebServer/cppBackend/reactor/Connection.h#L26-L132) | `Connection` 类 | 增加 `Task<size_t> AsyncRead()`、`Task<size_t> AsyncWrite()`、`Task<void> AsyncSendFile()`。 |
| [Connection.cpp](file:///home/zsy/WebServer/cppBackend/reactor/Connection.cpp#L243-L381) | `onmessage()` | 将读循环包装为 awaitable；TLS `WANT_READ/WANT_WRITE` 转成 `co_await` 事件。 |
| [Connection.cpp](file:///home/zsy/WebServer/cppBackend/reactor/Connection.cpp#L86-L241) | `writecallback()` | 将 writev/sendfile/TLS 回写状态改为协程驱动发送循环。 |
| [HttpServer.h](file:///home/zsy/WebServer/cppBackend/reactor/HttpServer.h#L35-L196) | `HttpServer` | 增加 `Task<void> HandleMessageCoro(...)`、`Task<WorkResult> BuildResponseCoro(...)`。 |
| [HttpServer.cpp](file:///home/zsy/WebServer/cppBackend/reactor/HttpServer.cpp#L92-L165) | `HandleMessage()` | 保留背压逻辑，worker入口改为提交协程任务。 |
| [HttpServer.cpp](file:///home/zsy/WebServer/cppBackend/reactor/HttpServer.cpp#L167-L470) | `HandleMessageInWorker/PostResultToIoLoop` | 改为 `co_await` 版 pipeline，去掉大量状态搬运代码。 |
| [HttpFacade.h](file:///home/zsy/WebServer/cppBackend/http/include/HttpFacade.h#L38-L117) | `Process*` 接口 | 新增 `Task<HttpServerResult> ProcessPendingAsync(...)`。 |
| [HttpFacade.cpp](file:///home/zsy/WebServer/cppBackend/http/src/HttpFacade.cpp#L175-L359) | parse/validate/route 阶段 | 当前同步逻辑保留，外层包装为可 `co_await`。 |
| [sqlconnpool.h](file:///home/zsy/WebServer/cppBackend/mysql/sqlconnpool.h#L12-L35) | `SqlConnPool` | 新增 `Task<MYSQL*> GetConnAsync()`，引入协程等待队列。 |
| [sqlconnpool.cpp](file:///home/zsy/WebServer/cppBackend/mysql/sqlconnpool.cpp#L39-L59) | `GetConn/FreeConn` | `sem_wait` 路径改为可挂起 await，不阻塞 worker 线程。 |
| [StaticFileService.h](file:///home/zsy/WebServer/cppBackend/services/include/StaticFileService.h#L16-L24) | 服务接口 | 新增 `Task<bool> HandleStaticFileAsync(...)`。 |
| [StaticFileService.cpp](file:///home/zsy/WebServer/cppBackend/services/src/StaticFileService.cpp#L42-L147) | `stat/range/sendfile` | 将耗时文件元信息与MD5计算下沉为协程任务。 |
| [DownloadService.h](file:///home/zsy/WebServer/cppBackend/services/include/DownloadService.h#L16-L33) | 服务接口 | 新增 `Task<bool> HandleDownloadAsync(...)`。 |
| [DownloadService.cpp](file:///home/zsy/WebServer/cppBackend/services/src/DownloadService.cpp#L96-L211) | 下载主流程 | range/etag 逻辑保留，文件探测/哈希/直传改为 await 流程。 |
| [AuthService.cpp](file:///home/zsy/WebServer/cppBackend/services/src/AuthService.cpp#L61-L138) | 注册/登录 | `PBKDF2` 和 DAO 调用改为协程化线程池 offload。 |

### 3.3 方案A预期收益
- QPS：+30% ~ +50%
- P95：-20% ~ -35%
- P99：-20% ~ -40%
- 风险：低到中；与现有代码兼容最好。

---

## 4. 方案 B（折中）：folly::coro + 保留 Reactor，替换 Worker 调度层

### 4.1 改造思路
- IO 多路复用仍用现有 `EventLoop`。
- `ThreadPool` 任务执行层替换为 `folly::CPUThreadPoolExecutor + folly::coro::Task`。
- 将 `HandleMessageInWorker`、`AuthService`、`DownloadService` 迁到 `folly::coro::Task`。

### 4.2 方案B逐文件改造清单
| 文件 | 位置 | 改动内容 |
|---|---|---|
| [ThreadPool.h](file:///home/zsy/WebServer/cppBackend/reactor/ThreadPool.h#L15-L35) | 类定义 | 保留兼容壳，内部替换为 folly executor，支持 `schedule()`。 |
| [ThreadPool.cpp](file:///home/zsy/WebServer/cppBackend/reactor/ThreadPool.cpp#L3-L79) | 构造/执行/stop | 用 executor 替代条件变量队列；任务由 coroutine promise 调度。 |
| [HttpServer.cpp](file:///home/zsy/WebServer/cppBackend/reactor/HttpServer.cpp#L161-L470) | worker链路 | `addtask(lambda)` 改为 `co_withCancellation` 任务。 |
| [Connection.cpp](file:///home/zsy/WebServer/cppBackend/reactor/Connection.cpp#L86-L241) | 写路径 | 回写异常统一通过 `folly::Try` 上抛并落日志。 |
| [sqlconnpool.cpp](file:///home/zsy/WebServer/cppBackend/mysql/sqlconnpool.cpp#L39-L59) | 连接获取 | 包装 `co_reschedule_on_current_executor` + 阻塞隔离线程池。 |
| [AuthService.cpp](file:///home/zsy/WebServer/cppBackend/services/src/AuthService.cpp#L26-L138) | 哈希+查库 | 引入 coroutine 任务，拆分 CPU 密集和 IO 密集段。 |
| [cppBackend/CMakeLists.txt](file:///home/zsy/WebServer/cppBackend/CMakeLists.txt#L1-L239) | 链接层 | 引入 folly 依赖、`-DFOLLY_HAS_COROUTINES=1`。 |

### 4.3 方案B预期收益
- QPS：+25% ~ +45%
- P95：-18% ~ -30%
- P99：-20% ~ -35%
- 风险：中；依赖栈最重。

---

## 5. 方案 C（全量重构）：libunifex Sender/Receiver 管道化

### 5.1 改造思路
- 用 Sender/Receiver 重写“网络IO→协议解析→业务处理→回写”。
- `ThreadPool` 退场，统一执行器（IO/CPU/DB）+ 结构化并发（`async_scope`）。
- 适配“允许大幅改造”的需求，长期演进最佳。

### 5.2 方案C逐文件改造清单
| 文件 | 位置 | 改动内容 |
|---|---|---|
| [Eventloop.h](file:///home/zsy/WebServer/cppBackend/reactor/Eventloop.h#L21-L68) | EventLoop | 提供 `sender` 化的 FD 就绪事件源。 |
| [Eventloop.cpp](file:///home/zsy/WebServer/cppBackend/reactor/Eventloop.cpp#L31-L108) | `run/queueinloop/handlewakeup` | 变为 operation-state 驱动，减少回调分叉。 |
| [Connection.h](file:///home/zsy/WebServer/cppBackend/reactor/Connection.h#L26-L132) | Connection | 增加 `ReadSender/WriteSender/SendFileSender`。 |
| [Connection.cpp](file:///home/zsy/WebServer/cppBackend/reactor/Connection.cpp#L243-L381) | 读路径 | `onmessage()` 改为 sender 发射字节块。 |
| [HttpServer.cpp](file:///home/zsy/WebServer/cppBackend/reactor/HttpServer.cpp#L92-L470) | 主处理链 | 改为 `let_value | then | upon_error` 的全异步管道。 |
| [HttpFacade.cpp](file:///home/zsy/WebServer/cppBackend/http/src/HttpFacade.cpp#L175-L359) | parse/route | 作为 pipeline 节点 sender。 |
| [sqlconnpool.cpp](file:///home/zsy/WebServer/cppBackend/mysql/sqlconnpool.cpp#L39-L59) | DB访问 | 改为 `query_sender`，连接池争用通过异步等待队列处理。 |
| [StaticFileService.cpp](file:///home/zsy/WebServer/cppBackend/services/src/StaticFileService.cpp#L42-L147) | 文件服务 | 变为 sender 节点，range/etag 与 sendfile 分离。 |
| [DownloadService.cpp](file:///home/zsy/WebServer/cppBackend/services/src/DownloadService.cpp#L96-L211) | 下载服务 | 构建可组合断点续传 sender 流。 |
| [tcpserver.cpp](file:///home/zsy/WebServer/cppBackend/reactor/tcpserver.cpp#L45-L74) | 新连接分发 | 连接生命周期纳入 `async_scope`。 |

### 5.3 方案C预期收益
- QPS：+40% ~ +70%
- P95：-30% ~ -50%
- P99：-35% ~ -55%
- 风险：高；重构范围最大。

---

## 6. CMake 编译链完整改造（C++20）

### 6.1 必改项
- 顶层 `C++17 -> C++20`：
  - [CMakeLists.txt](file:///home/zsy/WebServer/CMakeLists.txt#L4-L6)
  - [cppBackend/CMakeLists.txt](file:///home/zsy/WebServer/cppBackend/CMakeLists.txt#L4-L6)
- 新增协程编译选项（GCC/Clang）：
  - `target_compile_features(... cxx_std_20)`
  - GCC11+ 通常无需显式 `-fcoroutines`，但可按编译器探测补充。
- 按方案接入库：
  - A：`FetchContent(cppcoro)`
  - B：`find_package(folly CONFIG REQUIRED)`（或子模块）
  - C：`FetchContent(libunifex)`
- 对 `http` 静态库和主程序统一开启 C++20：
  - [http/src/CMakeLists.txt](file:///home/zsy/WebServer/cppBackend/http/src/CMakeLists.txt#L82-L93)
  - [cppBackend/CMakeLists.txt](file:///home/zsy/WebServer/cppBackend/CMakeLists.txt#L217-L239)

### 6.2 建议新增 CMake 结构
- `cmake/CoroutineOptions.cmake`：集中处理标准、警告、sanitize。
- `cppBackend/coroutine_runtime/`：协程运行时（Task/Executor/Awaiter）。
- 目标建议：
  - `add_library(coro_runtime STATIC ...)`
  - `target_link_libraries(httpserver PRIVATE coro_runtime http OpenSSL::SSL OpenSSL::Crypto ...)`

---

## 7. 性能基准测试方案（同负载对比）

### 7.1 测试场景
- 场景1：静态小文件（4KB，keep-alive）
- 场景2：动态登录（含 DB + PBKDF2）
- 场景3：大文件下载（100MB，Range/HEAD）
- 场景4：混合流量（60%静态 + 30%API + 10%下载）

### 7.2 固定压测参数（同负载）
- 工具：`wrk` + `h2load`
- 示例：
  - `wrk -t16 -c1000 -d60s --latency http://127.0.0.1:8080/index.html`
  - `wrk -t16 -c500 -d60s --latency -s post_login.lua http://127.0.0.1:8080/login`
  - `h2load -c100 -m10 -d60 http://127.0.0.1:8080/download/test.bin`
- 指标：QPS、P95、P99、CPU%、上下文切换、连接池等待时长。

### 7.3 量化提升预期
| 指标 | 改造前基线 | 方案A | 方案B | 方案C |
|---|---:|---:|---:|---:|
| QPS | 1.0x | 1.30x ~ 1.50x | 1.25x ~ 1.45x | 1.40x ~ 1.70x |
| P95 | 1.0x | 下降 20% ~ 35% | 下降 18% ~ 30% | 下降 30% ~ 50% |
| P99 | 1.0x | 下降 20% ~ 40% | 下降 20% ~ 35% | 下降 35% ~ 55% |

---

## 8. 单元测试与集成测试方案

### 8.1 利用现有测试目录扩展
- 现有测试入口：[test/CMakeLists.txt](file:///home/zsy/WebServer/test/CMakeLists.txt#L1-L236)

### 8.2 建议新增测试用例（最小集合）
- `coroutine_eventloop_unit_tests.cpp`
  - 验证 await fd-ready、取消、超时恢复顺序。
- `coroutine_http_pipeline_tests.cpp`
  - 覆盖 `NEED_MORE_DATA`、错误映射、乱序结果回写。
- `coroutine_db_pool_tests.cpp`
  - 覆盖连接池耗尽/恢复、公平性、无死锁。
- `coroutine_file_service_tests.cpp`
  - 覆盖 `Range/HEAD/ETag/If-Modified-Since`。
- `coroutine_stress_integration_tests.cpp`
  - N 并发连接下验证 P99 与错误率稳定性。

### 8.3 必测回归点（基于当前代码）
- TLS 握手与回退：[Connection.cpp](file:///home/zsy/WebServer/cppBackend/reactor/Connection.cpp#L248-L345)
- 背压与503策略：[HttpServer.cpp](file:///home/zsy/WebServer/cppBackend/reactor/HttpServer.cpp#L122-L144)
- sendfile 回写路径：[Connection.cpp](file:///home/zsy/WebServer/cppBackend/reactor/Connection.cpp#L203-L239)

---

## 9. 异常处理与调试指南

### 9.1 异常处理规范
- 协程边界统一捕获异常并转换为 `HttpError/500`，禁止异常跨线程裸传。
- 所有 `Task` 在 `final_suspend` 前写入统一日志字段：`request_id/coro_id/fd/path/stage/error_code`。
- DB/file await 必须支持超时与取消（避免悬挂协程）。
- 连接关闭时取消关联协程，避免 use-after-free（重点关联 [Connection.h](file:///home/zsy/WebServer/cppBackend/reactor/Connection.h#L45-L55) 上下文对象）。

### 9.2 调试方法
- 日志级别：
  - IO：epoll事件、resume/suspend 次数
  - 业务：请求生命周期分段耗时
  - 异常：协程栈摘要 + request_id
- 建议开启：
  - Debug：`-O0 -g3 -fsanitize=address,undefined`
  - Perf：`perf record + flamegraph` 观察协程调度热点
- 重点观察点：
  - `EventLoop::handlewakeup` 延迟：[Eventloop.cpp](file:///home/zsy/WebServer/cppBackend/reactor/Eventloop.cpp#L91-L108)
  - worker链路排队：[HttpServer.cpp](file:///home/zsy/WebServer/cppBackend/reactor/HttpServer.cpp#L446-L469)
  - DB 池等待：[sqlconnpool.cpp](file:///home/zsy/WebServer/cppBackend/mysql/sqlconnpool.cpp#L39-L53)

---

## 10. 实施建议（推荐落地顺序）
1. Phase-1（2周）：落地方案A基础框架（Task/Scheduler/Awaiter）+ `HttpServer` 主链路协程化。
2. Phase-2（2周）：DB 与文件服务协程化，压测并达成 QPS +30%、P99 -20% 目标。
3. Phase-3（可选3-6周）：如果追求更高上限，升级到方案C（libunifex 全链路）。

